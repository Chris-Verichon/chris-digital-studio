"use client";

/**
 * Site-wide contact configuration.
 * Replace these placeholders with the real handles before going live.
 */
export const siteConfig = {
  email: "hello@chris.studio",
  // Calendly scheduling URL embedded in the contact section.
  calendlyUrl: "https://calendly.com/your-handle/30min",
} as const;
